name: Prepare Production Release

on:
  workflow_dispatch:
    inputs:
      revision:
        description: Reviewed full commit SHA with successful CI
        required: true
        type: string

permissions:
  contents: read
  actions: read

jobs:
  package:
    uses: ./.github/workflows/package-release.yml
    with:
      revision: ${{ inputs.revision }}
      environment: production
