global:
  scrape_interval: 15s

rule_files:
  - /etc/prometheus/prometheus-rules.yml

scrape_configs:
  - job_name: 'fastapi_backend'
    scrape_interval: 5s
    static_configs:
      - targets: ['backend:8000']
